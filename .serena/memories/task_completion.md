# opencode-token-reduce — Task Completion

After completing work:

1. Verify with `npm test` or `bun test` if applicable
2. Check for any lint/type errors
3. Update relevant Serena memories if project structure or conventions changed
4. Commit and push if requested

No specific test framework — check README or package.json for test commands.